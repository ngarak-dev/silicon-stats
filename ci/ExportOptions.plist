# Export options for Developer ID / App Store archives (optional).
# CI's default path uses `xcodebuild build` + ad-hoc signing via scripts/build-dmg.sh.
# When DEVELOPMENT_TEAM and a Developer ID Application certificate are available:
#   xcodebuild archive ... -archivePath build/SiliconStats.xcarchive
#   xcodebuild -exportArchive -archivePath ... -exportPath dist -exportOptionsPlist ci/ExportOptions.plist

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>developer-id</string>
	<key>signingStyle</key>
	<string>automatic</string>
	<key>destination</key>
	<string>export</string>
</dict>
</plist>
